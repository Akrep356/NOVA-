<?xml version="1.0" encoding="utf-8"?>

<manifest
xmlns:android="http://schemas.android
.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="NOVA"
        android:supporysRtl="true"

android:theme="@android:style/Theme
.Metarial.Light.NoActionBar">

        <activity

android:name="com.example.nova.MainAct
iviy"
            android:exported="true"

            <intent-filter>

                <action
android:name="android.intent.action.MA
IN" />

                <category
android:name="android.intent.category.
LAUNCHER" />

            </intent-filter>

        </activity>

    </application>

</manifest>
